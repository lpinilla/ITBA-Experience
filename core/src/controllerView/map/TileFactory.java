package controllerView.map;

/**
 * Created by agustin on 18/06/17.
 */
public class TileFactory{

    /*public static enum Tiles {
        ground, wall, furniture, passenger, combat, library:
    }

    public static Tile createTile(Tiles tTile){
        switch(tTile){
            case ground:
                return new GroundTile();
            case wall:
                return new WallTile();
            case furniture:
                return new FurnitureTile();
            case passenger:
                return new PassengerTile();
            case combat:
                return new CombatTile();
            case library:
                return new LibraryTile();
        }
    }*/
/*
    private class GroundTile extends Tile{
        public GroundTile(){
            super(true, false, new GroundType());
        }
    }

    private class WallTile extends Tile{
        public WallTile(){
            super(false, false, new WallType());
        }
    }

    private class FurnitureTile extends Tile{
        public FurnitureTile(){
            super(false, false, new FurnitureType());
        }
    }

    private class PassengerTile extends Tile{
        public PassengerTile(){
            super(true, false, new PassengerType());
        }
    }

    private class CombatTile extends Tile{
        public CombatTile(){
            super(true, false, new CombatTile());
        }
    }

    private class LibraryTile extends Tile{
        public LibraryTile(){
            super(true, true, new LibraryType());
        }
    }

*/
}
